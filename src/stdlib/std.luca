// luca standard library

import "std-ast.luca" in
export let std-abs-int = \a:int. if a < 0 then 0 - a else a in
export let std-clamp-int = \x:int. \lo:int. \hi:int.
  if x < lo then lo else if x > hi then hi else x in
()
